spring:
  application:
    name: salesmanager4
  # datasource:
  #   url: jdbc:postgresql://localhost:5432/salesmanager4?currentSchema=sales_manager
  #   username: postgres
  #   password: lahiru
  #   driver-class-name: org.postgresql.Driver
  datasource:
         url: jdbc:h2:mem:dcbapp;MODE=PostgreSQL;;DATABASE_TO_UPPER=FALSE;CASE_INSENSITIVE_IDENTIFIERS=TRUE
         driver-class-name: org.h2.Driver
         username: sa
         password: password
  h2:
    console:
      enabled: true



logging:
  level:
    "[org.springframework.jdbc.core.JdbcTemplate]": DEBUG
    web: DEBUG