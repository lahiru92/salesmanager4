spring:
  application:
    name: salesmanager4
  # datasource:
  #   url: jdbc:postgresql://localhost:5432/salesmanager4?currentSchema=sales_manager
  #   username: postgres
  #   password: lahiru
  #   driver-class-name: org.postgresql.Driver
  datasource:
         url: jdbc:h2:mem:dcbapp;MODE=PostgreSQL;
         driver-class-name: org.h2.Driver
         username: sa
         password: password
  h2:
    console:
      enabled: true
      path: /h2-console



logging:
  level:
    "[org.springframework.jdbc.core.JdbcTemplate]": DEBUG